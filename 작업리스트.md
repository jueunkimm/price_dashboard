# 가전 가격트래킹 대시보드 — 작업 리스트

> 기획서(`가전_가격트래킹_대시보드_기획서.md`) 분석 기반 구현 작업 분해
> 작성일: 2026-06-01

---

## 📌 진행 현황 (2026-06-01 업데이트)

**Phase 1 (MVP) — ✅ 완료, 실데이터 가동 중**
- 스캐폴딩/DB/수집기/집계 API/대시보드 화면 전부 구현·검증
- 네이버쇼핑 실수집: 31개 카테고리 × 50 = 1,550 스냅샷
- 쿠쿠 매칭 83개(오탐 0), 렌탈 43개 분리 → 포지셔닝 왜곡 제거
- 데모용 전일 스냅샷 백필로 변동률/랭킹/추세 활성화

**A. 쿠쿠 매칭 정확도 보강 — ✅ 완료**
- brand_raw 우선 + 제목 토큰 매칭(신뢰도/사유 메타), 렌탈 플래그 도입·집계 제외

**B. Phase 1 UI 다듬기 — ✅ 완료**
- 렌탈 배지, 수집 현황, 쿠쿠 포지셔닝 패널, 카테고리 클릭 필터, 반응형

**C. Phase 2 착수 — 🟡 부분 완료**
- ✅ F10 이벤트 캘린더(완전), ✅ F7 수요 트렌드(백엔드 완성+데모 데이터; **데이터랩 권한 추가 시 실데이터**)
- ⬜ F8 실질가 / F9 채널비교: 데이터 소스(카드할인·다채널) 확보 전까지 모델 필드만 스캐폴딩

**후속 ① 쿠쿠 매칭 정밀도(방법 B) — ✅ 완료**
- 데이터 기반 모델코드 사전(`cuckoo_models.py`) + 카테고리 게이팅. 단, 코드형식은
  경쟁사(쿠첸·코웨이·코지마) 공유 → brand_raw 빈 경우로만 적용. 매칭 83개·오탐 0.

**후속 ③ Phase 3 — 🟡 대부분 완료**
- ✅ F11 알림: 규칙 엔진 + 인앱 알림(643건) + 패널. 외부발송(이메일/슬랙)은 자격증명·동의 필요로 스캐폴딩.
- ✅ F12 환율: macro_metric + ECOS 클라이언트 + 데모 환율 + USD/KRW 칩. 실데이터는 ECOS 키 필요.
- ✅ F13 주간 리포트: 집계 리포트 + API + UI 패널 + 마크다운 출력.

**QA 1차 조치(1+2단계) — ✅ 완료** (상세: `QA_리포트.md`)
- 신뢰: 합성 표식+신뢰성 배너, 부품/렌탈 제외, 모델 dedup, 데이터품질 API
- 의사결정: 동급(용량) 포지셔닝, 경쟁 브랜드 24종·52% 매핑·브랜드 비교 패널
- 품질: 합성 라벨 버그 수정, pytest 21개 통과

**다음 액션(사용자)**: ① 네이버 "데이터랩" 권한 → F7 실데이터, ② ECOS 키 → F12 실데이터,
③ 쿠쿠 공식 모델 리스트 → 매칭 사전 확장, ④ 이메일/슬랙 설정 → F11 외부발송,
⑤ 실수집 N일 누적 → 변동률 실데이터화(현재 variation_ready=false)

**자동 QA + UI/UX 개선 — ✅ 완료**
- 자동 QA: Stop 훅(`.claude/settings.json`) + `backend/qa_check.py`(pytest+무결성, 매 턴 자동)
- UI/UX 1차: 내부 기능코드(F7/B-2 등) 제거, 로딩 스켈레톤, 추세 기간토글(7/30/90/전체)+데이터부족 안내,
  헤더 기준일, 카테고리 클릭 힌트
- UI/UX 2차: **제품 검색**(헤더 모델명 검색→추세 연결), **CSV 내보내기**(변동랭킹·브랜드비교, Excel 한글 BOM),
  **탭 구조**(시장 현황 / 쿠쿠 분석 / 알림·이벤트)로 스크롤 분산 (C-4/C-5 부분 해소)
- UI/UX 3차: **사이드바 필터**(카테고리·용량 상세구분·브랜드·가격대·렌탈),
  용량 추출 확장(kg·형/인치, 커버리지 357), KPI 최대 등락, 색상 범례
- UI/UX 4차: 사이드바를 **시장 현황 탭에 통합**(좌측 상시) → 카테고리 선택 시 상세(브랜드비교·제품목록·수요·추세),
  미선택 시 개요(카테고리 그리드·랭킹). 별도 '제품 탐색' 탭 제거(중복). 반응형(md+) 좌측컬럼·로딩 스켈레톤.
  실데이터 전환: **수요(데이터랩)·환율(ECOS)** 완료 — 합성은 변동률 백필만 남음
- 규격 정밀도 고도화: **카테고리별 규격 축 정의**(`app/spec.py`) — 밥솥=인용, TV=형, 세탁=kg,
  인덕션=구, 사운드바=채널, 커피=Bar, 헤어/청소기=W 등. 카테고리에 맞는 단위만 추출(오인식 제거).
  커버리지 357→390, 정수기·의류관리기 잘못된 추출 제거. 테스트 33개.
- **채널/몰 비교(F9 1차)**: 네이버 경유 몰(source) 데이터 활용 — 제품 목록에 판매몰 컬럼(쿠팡 배지),
  사이드바 **판매몰(채널) 필터**(쿠팡만 보기 등), 몰 분포(`/api/filter-options` malls). 쿠팡 데이터 이미 확보(네이버 경유).
  쿠팡 전용 대량 수집은 쿠팡 파트너스 API 필요(미진행, ToS 확인 전제).
- **수요 신호 이원화(F7 보강)**: 검색어트렌드(통합검색) + **쇼핑인사이트(쇼핑 클릭=구매의도)** 둘 다 수집·표시.
  `shopping_insight_client.py`(키워드별, cid 디지털가전/생활건강), demand 패널에 2선 오버레이.
- **쿠쿠 공식 카탈로그(방법 B)**: `productlist.xlsx`(1231모델) → `cuckoo_model` 적재(978개, 카테고리 매핑·별매품).
  매처에 카탈로그 정확매칭 최우선(0.99·권위). **쿠쿠 직접 수집**(`collect_cuckoo.py`)으로 신규 671개 추가 →
  **쿠쿠 83→754개**(완전 라인업), 카테고리 권위 분류(밥솥 280·인덕션·정수기 등). 스케줄러 월·금 자동.
- UI/UX 5차: 사이드바 → **상단 접이식 필터 바**(`FilterBar`, 기본 접힘, 접힘 시 적용필터 칩 요약) → 콘텐츠 전체폭.

**종합 평가(100점 기준)**: ≈72/100 — `평가_100점기준.md`. 90+는 실데이터 연결(신뢰성)이 결정적.

**미해결(데이터소스/추가구현 필요)**: B-3 실구매가, B-4 채널비교, C-3 사전집계 배치,
알림설정 UI, D-2 Alembic, 모바일/다크모드

---

## 0. 착수 전 확정 필요 사항 (Blocker)

구현 시작 전에 아래 3가지가 결정되어야 헛작업을 막을 수 있습니다.

- [ ] **0-1. 네이버 OpenAPI 키 발급** — 네이버 개발자센터에서 검색 API(쇼핑) Client ID/Secret 발급. (데이터랩/ECOS는 Phase 2~3)
- [ ] **0-2. DB 선택** — MVP는 **SQLite**(로컬·간편) vs **PostgreSQL**(Docker, 기획서 권장). → 빠른 MVP라면 SQLite 시작 후 마이그레이션 권장.
- [ ] **0-3. 쿠쿠 모델 리스트 확정** — `is_own` 매칭 정확도 98% 목표(KPI). 실제 판매 모델/카테고리 매핑(★)과 브랜드 별칭 사전(쿠쿠/CUCKOO/쿠쿠전자/쿠쿠홈시스) 확정.

---

## Phase 1 — MVP (가격·변동률) ★우선

### A. 프로젝트 스캐폴딩
- [ ] A-1. 모노레포 구조 생성 (`/frontend`, `/backend`, `/collector`, `/db`)
- [ ] A-2. 백엔드: FastAPI 프로젝트 골격 + `.env` 설정(API 키 분리)
- [ ] A-3. 프론트: React + Vite + TypeScript + Tailwind + Recharts 초기화
- [ ] A-4. (Postgres 선택 시) docker-compose 작성 — postgres 포함
- [ ] A-5. 공통: 린트/포맷/기본 README

### B. 데이터 모델 (6장 스키마)
- [ ] B-1. 마이그레이션: `category`, `brand`, `product`, `price_snapshot` (MVP 핵심 4종)
- [ ] B-2. `brand` 시드: 쿠쿠 `is_own=true`, `aliases_json=["쿠쿠","CUCKOO","쿠쿠전자","쿠쿠홈시스"]`
- [ ] B-3. `category` 시드: 4.1장 전체 가전 카테고리(대/소분류) + 쿠쿠 라인업 ★태깅
- [ ] B-4. `product`에 `brand_id` FK, `brand_raw`, `is_own_brand` 컬럼 반영
- [ ] B-5. (확장 대비) `demand_metric`/`macro_metric`/`market_event`/`alert_rule` 스키마는 정의만 준비

### C. 수집기 (Collector)
- [ ] C-1. 네이버쇼핑 검색 API 클라이언트 (rate limit·User-Agent 준수)
- [ ] C-2. 카테고리별 상품·가격 수집 → `price_snapshot` 적재 (원본 스냅샷 보관)
- [ ] C-3. **브랜드 매칭 로직**: 별칭 사전으로 `brand_raw` → `brand_id`/`is_own_brand` 자동 세팅
- [ ] C-4. APScheduler 스케줄러(1일 1~2회) + 수집 로그 테이블/기록
- [ ] C-5. 제품 매칭 정규화(모델명 클린징) — 동일 모델 식별 오류 방지

### D. 집계·변동률 배치 + API
- [ ] D-1. 변동률 계산 배치: 일/주/월 (일간=(당일−전일)/전일×100)
- [ ] D-2. 카테고리 집계: 평균/**중앙값**/최저·최고가 (중앙값 권장 — 이상치 민감도↓)
- [ ] D-3. 이상치 감지: |변동률| ≥ 임계치(기본 10%, 설정 가능) 또는 z-score
- [ ] D-4. **쿠쿠 포지셔닝 계산**: (쿠쿠가 − 카테고리 평균가)/평균가 ×100
- [ ] D-5. REST API:
  - 카테고리 현황(평균/중앙값/변동률)
  - 변동 랭킹
  - 제품 시계열
  - 쿠쿠 포지셔닝
  - **모든 조회 API에 `is_own_brand` 필터 파라미터** 추가

### E. 대시보드 화면 (7장)
- [ ] E-0. 헤더 **글로벌 "전체 보기 / 쿠쿠만 보기" 토글** (켜면 전 패널 필터링)
- [ ] E-1. 상단 KPI 요약 바 (평균 변동률·급변 수·최대 등락 / 쿠쿠 모드 시 쿠쿠 지표)
- [ ] E-2. 카테고리 카드 그리드 (평균가+변동률 색상[상승 빨강/하락 파랑]+미니 스파크라인, ★표식, 쿠쿠 포지셔닝 뱃지)
- [ ] E-3. 추세 라인 차트 (기간 토글 7D/30D/90D/1Y, 쿠쿠 라인 강조색)
- [ ] E-4. 변동 랭킹 테이블 (정렬·필터·검색, "쿠쿠만" 필터 칩)
- [ ] E-5. 제품 검색·필터 (카테고리/브랜드/가격대/모델) + 사이드바(자사 전용 필터)
- [ ] E-6. 자사 제품 시각 구분(배지/색상) 공통 컴포넌트
- [ ] E-7. 수집 시각·소스 표기(신뢰성), 모바일 반응형

### F. 검증
- [ ] F-1. **더미 시드 데이터** 생성 (API 없이도 화면 동작 확인용)
- [ ] F-2. 통합 테스트 (수집→집계→API→화면)
- [ ] F-3. 브라우저 검증 (preview) — 토글/필터/차트 동작 확인

---

## Phase 2 — 시장조사 보완

- [ ] G-1. 네이버 데이터랩 트렌드 수집 + 수요 오버레이 뷰 (F7)
- [ ] G-2. 실질가(체감가) 환산 로직·필드 — 할인/쿠폰/사은품 반영 (F8)
- [ ] G-3. 채널 비교 뷰 — 동일 모델 채널별 가격·재고 (F9)
- [ ] G-4. 변동 원인 태깅 — 프로모션 캘린더 이벤트 마커 차트 표시 (F10)
- [ ] G-5. 쿠쿠 ↔ 경쟁 모델 1:N 가격·스펙 비교 뷰 (F-C2)

---

## Phase 3 — 인텔리전스/자동화

- [ ] H-1. 알림 규칙·발송 (이메일/슬랙) — 임계치 변동·신제품 감지 (F11)
- [ ] H-2. 환율/거시지표 오버레이 — 한국은행 ECOS API (F12)
- [ ] H-3. 주간 요약 리포트 자동 생성 + 리뷰 감성분석 (F13)

---

## 상시 준수 사항 (5.2 법적·운영)

- 공식 API 우선, 크롤링은 ToS·robots.txt 검토 + rate limit 준수
- 개인정보(리뷰 작성자 식별정보) 미수집
- API 키·수집 대상은 환경변수/설정으로 분리

## 핵심 KPI (10장)
- 수집 성공률 ≥ 95% / **쿠쿠 매칭 정확도 ≥ 98%** / 데이터 지연 ≤ 24h
