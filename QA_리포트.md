# QA 재분석 리포트 — PM 실무 활용 관점

> 기준: "가전 상품기획자(PM)가 **실제 가격 의사결정에 쓸 수 있는 수준**인가?"
> 작성일: 2026-06-01 · 근거: 실제 DB/코드 진단(`qa_diagnose.py` 결과)

## 🔧 1차 조치 결과 (1+2단계 반영 완료)

| 이슈 | 조치 |
|------|------|
| A-1 합성 데이터 격리 | ✅ `is_synthetic` 표식 + **데이터 신뢰성 배너**(실측일수/합성여부 고지) + `variation_ready` 플래그 |
| A-3 카테고리 오염 | ✅ 부품/소모품 `is_accessory` 25개 탐지·집계 제외 (호환/내솥/필터세트 등) |
| A-4 모델 단위 집계 | ✅ `model_key`로 몰별 중복 dedup(최저가 대표) |
| A-5 렌탈 잔존 | ✅ 랭킹·알림에서도 렌탈 제외 |
| B-1 like-for-like | ✅ 용량 파싱 + **동급(용량 구간) 포지셔닝** 추가 (단, 제목 용량 표기 한계로 커버리지 318/1548) |
| B-2 경쟁사 식별 | ✅ 경쟁 브랜드 24종 시드 → **807/1548(52%) 매핑**, **브랜드 비교 패널**(쿠쿠 vs 쿠첸 등) |
| C-2 합성 라벨 버그 | ✅ API `is_synthetic` 기반으로 수정, 환율 칩·수요 패널 일관 표기 |
| D-1 테스트 부재 | ✅ pytest **21개**(매처/정제 로직 회귀) 통과 |

**남은 항목**: A-2(데이터랩/ECOS 실데이터 — 사용자 권한 필요), B-3 실구매가, B-4 채널비교, C-3 사전집계 배치, C-4 검색/알림 UI, C-5 내보내기, D-2 Alembic, D-3 키 재발급. → 아래 본문 참조.

---

## 종합 판정

| 항목 | 현재 수준 |
|------|-----------|
| 엔지니어링 완성도 | 🟢 양호 (수집→집계→API→UI 파이프라인 동작, 빌드/타입체크 통과) |
| **PM 실무 활용 가능 여부** | 🔴 **아직 불가** — 아래 Critical 이슈로 "숫자를 믿고 의사결정"할 수 없음 |
| 핵심 격차 | 데이터 신뢰성(합성/오염) + 의사결정 적합성(like-for-like, 경쟁사, 실구매가) |

현재는 **기능 데모(PoC)** 수준이다. 화면·기능은 기획서를 충실히 반영했으나,
**보이는 숫자의 상당수가 합성이거나 비교 기준이 어긋나** PM이 그대로 인용하면 잘못된 판단을 내릴 수 있다.

---

## 심각도 정의
- 🔴 **Critical**: 잘못된 의사결정을 유발. 실무 투입 전 필수 해결.
- 🟠 **High**: PM 핵심 질문에 답 못 함. 실무 가치의 핵심.
- 🟡 **Medium**: 정확도/사용성 저하. 신뢰·효율 영향.
- ⚪ **Low**: 운영/품질 위생.

---

## A. 데이터 신뢰성 (실무 사용의 1차 관문)

### A-1. 🔴 변동률·추세·알림이 사실상 합성 데이터
- **증거**: 가격 스냅샷 **2일치**(실측 1일 + `backfill_demo` 합성 1일). 변동률/급변 576건/알림 643건/추세 차트는 **모두 합성 전일가 기반**.
- **영향**: PM이 "오늘 -15% 급락" 알림을 보고 대응하면 → 실제로는 가짜.
- **수정**: 합성 백필 제거하고 **실수집 N일 누적**으로만 변동률 산출. 누적 전까지 UI에 "변동 데이터 수집 중(Dn/D2)" 배지 표기. 데모 데이터는 별도 토글로 격리.

### A-2. 🔴 수요(F7)·환율(F12) 전량 합성
- **증거**: `demand_metric` 2,790행 전부 `demo_synthetic`, `macro_metric` 전부 합성.
- **영향**: "가격↓ + 검색량↑" 같은 핵심 해석(2.2-A)이 가짜 신호.
- **수정**: 네이버 데이터랩 권한 / ECOS 키 확보 후 실데이터 교체. **합성인 동안 화면에 명확한 경고 라벨**(현재 수요 패널만 경고, 환율 칩·리포트엔 없음 → 버그 C-2).

### A-3. 🔴 카테고리 가격 풀의 비교 오염 (악세서리·미니·타급 혼입)
- **증거**:
  - 전기밥솥 가격범위 24,200 ~ 410,360원(**17배**). 최저가에 "키친아트 미니밥솥 1인용".
  - 정수기 최저가 9,900원(렌탈 자가관리), 공기청정기 최저가 34,800원(미니 살균기).
- **영향**: 카테고리 평균·중앙값·최저가가 **비교 불가능한 품목**으로 오염 → 쿠쿠 포지셔닝 ±%가 무의미.
- **수정**:
  1. 검색 결과 필터링(부품/호환/케이스/필터 키워드 제외, 가격 하한, `category3` 매칭).
  2. 용량·등급 등 **스펙 정규화 후 동급끼리 비교**(아래 B-1).

### A-4. 🟠 모델 단위가 아닌 "쇼핑몰 리스팅" 단위 집계
- **증거**: 전기밥솥에서 동일 모델코드(CRP-CHP1010FD) 2개 product. 네이버 productId는 몰별로 분리되어 **같은 모델이 중복 카운트**.
- **영향**: 카테고리 "제품 수"·평균이 리스팅 수에 좌우. 모델 수 ≠ 표시 수.
- **수정**: 모델코드/정규화 모델명 기준 **모델 마스터 테이블**로 dedup, 모델당 최저가 1개로 집계.

### A-5. 🟡 렌탈 분리 불완전
- **증거**: 변동 랭킹 1,546건 중 **렌탈 43건 포함** → 월렌탈료 변동이 알림화 가능. (집계 평균에서만 제외, 랭킹/알림엔 잔존)
- **수정**: 랭킹·알림에서도 렌탈 제외 옵션, 또는 렌탈은 "월요금" 별도 축으로 분리. 키워드 방식의 정밀도도 검수 필요.

---

## B. PM 의사결정 적합성 (실무 가치의 핵심)

### B-1. 🟠 like-for-like 비교 부재 (스펙 미정규화)
- **증거**: 쿠쿠 밥솥 60,010~410,360원이 "전체 평균" 한 값과 비교됨. 6인 vs 10인 vs 미니가 뒤섞임.
- **영향**: "쿠쿠 정수기 +0.61%" 같은 수치가 **동급 비교가 아니라** PM이 가격전략 근거로 못 씀.
- **수정**: `spec_json`에 용량·등급 파싱(예: 10인용/6인용) → **동급 세그먼트 내 포지셔닝**.

### B-2. 🟠 경쟁사 식별 부재
- **증거**: `brand` 테이블에 쿠쿠만 등록. 그 외는 전부 "비자사"로 뭉뚱그려짐.
- **영향**: "삼성·LG·쿠첸·코웨이가 언제 얼마 내렸나"(프로모션 대응 핵심)에 **답 불가**.
- **수정**: 주요 경쟁 브랜드 alias 사전 등록 → 브랜드별 가격/변동/점유 비교 뷰(F-C2 1:N 비교).

### B-3. 🟠 실구매가(실질가) 미반영 (F8)
- **증거**: `effective_price` 컬럼만 존재, 계산 로직 없음. 표시가(lprice)만 사용.
- **영향**: 기획서 2.1이 명시한 "표시가 vs 체감가 괴리" → PM이 체감가로 판단 불가.
- **수정**: 카드할인/쿠폰/사은품 데이터 소스 확보 후 환산. (네이버 검색 API 미제공 → 추가 소스 필요)

### B-4. 🟡 채널 비교 부재 (F9) / 단일 채널
- **증거**: 채널 = naver 단일. `channel`/`in_stock` 필드만 스캐폴딩.
- **영향**: "어디서 최저가/품절인가" 답 불가.
- **수정**: 다채널 수집(ToS·법무 검토 후) 또는 네이버 내 몰별 분포 활용.

### B-5. 🟡 추세 기간 토글 없음 (F3 미완)
- **증거**: TrendChart에 7D/30D/90D/1Y 토글 없음(기획서 7.1-3).
- **수정**: 기간 필터 + 이벤트 마커 오버레이(F10 연계).

---

## C. 기능 결함 / 버그

### C-1. 🟡 변동 랭킹·알림에 렌탈 혼입 → 오인 알림 (A-5와 연계)

### C-2. 🟡 수요 패널 "합성" 라벨 로직 오류
- **증거**: `DemandPanel.tsx`의 `synthetic = series.length > 0` → **실데이터로 교체해도 항상 "데모용 합성" 경고** 표시. 반대로 환율 칩·주간리포트엔 합성 경고가 **아예 없음**.
- **수정**: API에 `source`(synthetic/real) 노출 후 라벨을 출처 기반으로. 환율/리포트에도 동일 처리.

### C-3. 🟡 집계 성능 — 매 요청마다 전체 스냅샷 재계산
- **증거**: `aggregation`이 KPI/카테고리/랭킹 호출마다 모든 product+snapshot을 메모리로 로드해 O(n) 재계산. 캐시·사전집계 없음.
- **영향**: 데이터가 수개월 누적되면 응답 지연. 기획서 9장의 "변동률 계산 배치"가 미구현(온더플라이로 대체).
- **수정**: 일배치로 변동률/집계 사전계산 테이블 적재 또는 캐시.

### C-4. 🟡 사이드바·검색·알림설정 UI 미구현
- **증거**: 기획서 7.1-6 사이드바(브랜드/기간/자사/알림 설정), E-5 검색창 미구현. 카테고리 클릭 필터만 존재. 알림 규칙은 읽기 전용(생성/수정 불가).
- **수정**: 검색 입력, 브랜드/가격대/기간 필터, 알림 규칙 CRUD UI.

### C-5. ⚪ 내보내기(Excel/CSV) 없음
- PM 실무는 보고서·엑셀 중심. 카테고리/랭킹/리포트 CSV·xlsx 내보내기 필요.

### C-6. ⚪ 타임존 경계
- `collected_at`이 UTC `now()`. 일자 그룹핑이 UTC 기준 → KST 자정 경계에서 하루가 갈릴 수 있음. KST 고정 권장.

---

## D. 운영 / 보안 / 품질 위생

- D-1. 🟡 **자동화 테스트 0** — 기획서 F2 "통합 테스트" 미작성. 매처/집계/렌탈/포지셔닝 회귀 테스트 필요(이미 매칭 로직에서 오탐 2회 발생 → 테스트 가치 큼).
- D-2. 🟡 **스키마 관리** — `create_all` + 수동 `migrate.py`. 운영 전환 시 Alembic 필요(컬럼 변경 누락 위험).
- D-3. 🟠 **API 키 노출** — 네이버 Client Secret이 채팅에 노출됨. **재발급 권장**(개발자센터 → 재발급 → `.env` 갱신).
- D-4. ⚪ **인증 부재** — API 무인증(로컬 단일 사용자엔 OK, 사내 공유 시 접근제어 필요).
- D-5. ⚪ **수집 견고성** — 카테고리 50개 고정·재시도/백오프 없음, 네이버 호출 실패 시 단순 skip. rate limit/쿼터 관리 로깅 보강 권장.

---

## PM 실무 활용까지의 로드맵 (우선순위)

**1단계 — 신뢰 확보 (Critical, 실무 투입 전제)**
1. 합성 데이터 격리 + 실수집 누적(A-1) / 합성 라벨 정합화(C-2)
2. 카테고리 품목 정제 — 부품·미니·렌탈·타급 필터링(A-3, A-5)
3. 모델 단위 집계(A-4)

**2단계 — 의사결정 가치 (High, 실무 핵심)**
4. 스펙 정규화 + 동급 포지셔닝(B-1)
5. 경쟁사 브랜드 식별 + 브랜드별 비교(B-2)
6. 데이터랩/ECOS 실데이터 연결(A-2)

**3단계 — 실무 편의 (Medium)**
7. 실구매가(B-3)·채널비교(B-4)·기간토글(B-5)
8. 검색/필터/알림설정 UI(C-4), 내보내기(C-5)
9. 사전집계 배치(C-3), 테스트(D-1), 키 재발급(D-3)

> **결론**: 현재는 "동작하는 데모". PM이 **실제 가격 의사결정에 인용**하려면 최소 1단계(신뢰)+2단계(동급비교·경쟁사) 완료가 필요하다. 그 전까지는 "참고용 모니터링" 수준으로만 사용 권고.
